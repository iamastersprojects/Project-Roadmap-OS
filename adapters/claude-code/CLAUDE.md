# {{PROJECT_NAME}} - Claude Code Adapter

Read `AGENTS.md`, `index.md`, recent `log.md`, and relevant project knowledge
before substantial work.

Use `.claude/commands/roadmap-start.md` for session orientation and
`.claude/commands/roadmap-repair.md` when tooling or methodology breaks.

Never auto-commit or auto-push. Human approval owns external writes.
