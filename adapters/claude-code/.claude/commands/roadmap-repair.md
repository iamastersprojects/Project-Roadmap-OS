---
description: Capture a broken tool, sandbox, CLI, MCP, or methodology path.
---

Reproduce the failure with the smallest safe fixture. Then run:

```text
roadmap-os repair --target . --component "<component>" --signal "<failure>"
```

Add a learning only if the correct route is reusable.
