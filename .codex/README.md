# Codex Adapter

Codex reads the root `AGENTS.md`. Start by running:

```text
node <roadmap-os-repo>/src/cli.mjs doctor --target .
```

Use `roadmap-os repair` whenever a reusable tooling or methodology breakage is
found during work.
